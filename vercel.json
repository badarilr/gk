{
  "version": 2,
  "installCommand": "echo 'Skipping install for prebuilt Flutter web output'",
  "buildCommand": "echo 'Skipping build for prebuilt Flutter web output'",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
